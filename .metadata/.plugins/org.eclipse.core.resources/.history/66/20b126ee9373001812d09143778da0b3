package codingoffer;

public class Solution15 {
    public ListNode FindKthToTail(ListNode head,int k) {
        if(head == null){
            return null;
        }
        ListNode pre = head;
        ListNode next = head;
        for(int i = 0; i < k; i++){
            if(next == null)
                return null;
            else
                next = next.next;
        }
        while(next != null){
            pre = pre.next;
            next = next.next;
        }
        return pre;
    }
}
